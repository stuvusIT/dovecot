#!/usr/bin/env bash

# {{ ansible_managed }}

exec rspamc \
{% if dovecot_rspamd_connect != "" %}
	--connect '{{ dovecot_rspamd_connect }}' \
{% endif %}
{% if dovecot_rspamd_password != "" %}
	--password '{{ dovecot_rspamd_password }}' \
{% endif %}
	learn_{{ item }}
